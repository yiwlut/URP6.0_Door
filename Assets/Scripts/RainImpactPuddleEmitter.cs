namespace DoorPuzzle
{
    // The former runtime Quad puddle emitter was removed. This inert marker
    // keeps Unity's cached compiler source list valid while an editor is open.
    internal static class RemovedPuddleEmitterMarker
    {
    }
}
